# ============================================
# GigFlow - Environment Variables Example
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to version control
# ============================================

# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================

# MongoDB connection string
# Replace with your MongoDB Atlas URI or local MongoDB connection
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# JWT secret key for signing and verifying authentication tokens
# MUST be at least 32 characters long for security
# Generate a strong random string (e.g., use: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_long

# Port the backend server will run on
# Default: 5000 (if not specified)
PORT=5000

# Frontend application URL (used for CORS and cookie settings)
# Development: http://localhost:5173
# Production: Your deployed frontend URL (e.g., https://your-app.onrender.com)
FRONTEND_URL=http://localhost:5173

# Application environment
# Use "development" for local development
# Use "production" for deployed environments
# Affects cookie security settings and error stack traces
NODE_ENV=development

# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# Note: Frontend uses Vite, so variables must be prefixed with VITE_
# Create a .env file in the frontend/ directory with:
# VITE_API_URL=http://localhost:5000
# ============================================
